{% extends 'layout.html.twig' %}

{% block title %}GRECO - Persona{% endblock %}

{% block content %}
    <div class="barraArriba">
        <h3 class="titulo">Cambiar contraseña</h3>
    </div>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="form-group row">
        <div class="col-sm-10">
            <a href="{{ path('welcome') }}" class="btn btn-secondary"><i class="fa fa-arrow-left"></i>Volver</a>

            <button type="submit" id="modificar_persona_enviar" name="guardar" class="btn-success btn">
                <i class="fa fa-check"></i>Guardar
            </button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}