{% extends 'layout.html.twig' %}

{% block title %}GRECO - Historial{% endblock %}

{% block content %}
    <div class="barraArriba">
        {% if historial.id %}
            <h2 class="titulo">Modificar Historial</h2>
        {% else %}
            <h2 class="titulo">Nuevo Historial</h2>
        {% endif %}
    </div>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <a href="{{ path('historial_listar') }}" class="btn btn-secondary"><i class="fa-solid fa-arrow-left"></i>Volver</a>
    <button type="submit" class="btn btn-success"><i class="fa fa-check"></i>Guardar</button>

    {% if historial.id %}
        <a href="{{ path('historial_eliminar', {'id': historial.id}) }}" class="btn btn-danger"><i class="fa-solid fa-trash-can"></i>Eliminar</a>
    {% endif %}

    {{ form_end(form) }}
{% endblock %}